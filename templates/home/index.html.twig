{% extends 'base.html.twig' %}

{% block title %}Dashboard - DocManager
{% endblock %}

{% block body %}
	<div class="page-header">
		<h1>🏠 Welcome to DocManager</h1>
	</div>

	<div class="stats-row">
		<div class="stat-card">
			<div class="stat-value">📄</div>
			<div class="stat-label">Documents</div>
		</div>
		<div class="stat-card">
			<div class="stat-value">👥</div>
			<div class="stat-label">Clients</div>
		</div>
		<div class="stat-card">
			<div class="stat-value">🔒</div>
			<div class="stat-label">Secure</div>
		</div>
	</div>

	<div class="card">
		<h2>Getting Started</h2>
		<p class="text-muted" style="margin-top: 0.5rem;">
			Manage your clients and their documents with ease. Use the navigation above to get started.
		</p>
		<div style="margin-top: 1.25rem; display: flex; gap: 0.75rem; flex-wrap: wrap;">
			{% if app.user %}
				<a href="{{ path('app_client_index') }}" class="btn btn-primary">👥 View Clients</a>
				<a href="{{ path('app_client_new') }}" class="btn btn-outline">+ New Client</a>
			{% else %}
				<a href="{{ path('app_login') }}" class="btn btn-primary">Sign In →</a>
				<a href="{{ path('app_register') }}" class="btn btn-outline">Create Account</a>
			{% endif %}
		</div>
	</div>
{% endblock %}
